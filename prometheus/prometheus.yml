global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'taskflow-django'
    static_configs:
      - targets: ['web:8000']

global:
  scrape_interval: 15s

rule_files:
  - "alert_rules.yml"

scrape_configs:
  - job_name: 'taskflow-django'
    static_configs:
      - targets: ['web:8000']